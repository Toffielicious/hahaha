export default function Mymessage({ messageko }) {
    return (
      <div>
        <p style={{backgroundImage:'linear-gradient(to right, blue,green)'}}>hawtdawg{messageko}</p>
        
      </div>
    );
  }
  